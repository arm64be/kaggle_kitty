[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "kaggle-kitty"
version = "0.1.0"
description = "Utility library for Kaggle notebooks"
requires-python = ">=3.8"
dependencies = ["requests"]
